<%= dynamic_form(commentable) do |f| %>
  <div class="form-group">
    <%= f.text_area :body, class: "form-control", placeholder: "Add a comment" %>
    <%= f.file_field :attachment, multiple: true %>
  </div>
  <%= f.submit class: "btn btn-outline-secondary" %>
<% end %>
